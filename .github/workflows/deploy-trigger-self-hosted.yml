on:
  push:
    branches:
      - acdh-1.9.6
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Install dependencies
        run: npm install --include=dev

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ vars.GHCR_USERNAME }}  
          password: ${{ secrets.GHCR_TOKEN }}   

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Deploy to Trigger.dev (Self-Hosted)
        env:
          TRIGGER_API_URL: ${{ vars.TRIGGER_API_URL }}
          TRIGGER_SERVER_URL: ${{ vars.TRIGGER_SERVER_URL }}
          TRIGGER_ACCESS_TOKEN: ${{ secrets.TRIGGER_ACCESS_TOKEN }}
          TRIGGER_PROJECT_ID: ${{ secrets.TRIGGER_PROJECT_ID }}
          TRIGGER_SECRET_KEY: ${{ secrets.TRIGGER_SECRET_KEY }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          IIIF_KEY: "dummy"
        run: |
          npx -y trigger.dev@4.4.6 login -a "$TRIGGER_SERVER_URL" --profile self-hosted
          npx -y trigger.dev@4.4.6 deploy --env staging --profile self-hosted -c ./trigger.config.ts   
